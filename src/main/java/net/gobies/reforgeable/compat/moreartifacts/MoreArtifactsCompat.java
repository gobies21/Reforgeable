package net.gobies.reforgeable.compat.moreartifacts;

/*
import net.gobies.moreartifacts.item.artifacts.AnkhShieldItem;
import net.gobies.moreartifacts.item.artifacts.CobaltShieldItem;
import net.gobies.moreartifacts.item.artifacts.ObsidianShieldItem;
import net.gobies.reforgeable.compat.curios.CuriosCompat;
import net.minecraft.world.item.ItemStack;
import net.minecraftforge.fml.ModList;

public class MoreArtifactsCompat {

    public static boolean isMAShield(ItemStack stack) {
        if (CuriosCompat.isLoaded() && ModList.get().isLoaded("moreartifacts")) {
            return stack.getItem() instanceof CobaltShieldItem || stack.getItem() instanceof ObsidianShieldItem || stack.getItem() instanceof AnkhShieldItem;
        }
        return false;
    }
}

 */
